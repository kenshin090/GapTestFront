import { BuiltinMethod } from '@angular/compiler';
import { Policy } from './Policy';

export class PolicyClient {  
    ClientId: number;
    Id: number;
    Policy: Policy;
    PolicyId: number;   
}

